receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

processors:
  batch:

exporters:
  otlphttp/backend:
    endpoint: "http://backend:8000"
    headers:
      X-API-Key: "ls_OR7qxIUkucF9uu7W2L0xrOOdnZLeCQcF82z9jxH_AcU"
    compression: none
    encoding: json

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp/backend]
